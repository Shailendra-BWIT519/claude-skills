# Toggles for claude-workflow/check.sh — true/false only, no script edits needed.
RUN_TYPECHECK=true
RUN_LINT=true
RUN_TEST=true
RUN_FORMAT=false

# Max lines of a failing check's output to print (repeated boilerplate from
# tools like Jest can otherwise dump hundreds of lines per failure).
MAX_OUTPUT_LINES=80
